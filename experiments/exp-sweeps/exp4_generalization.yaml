project: rxnflow_exp4
program: ./scripts/exp4_qed_opt.py
name: qed-opt

command:
  - ${env}
  - python
  - ${program}
  - qed # prefix
  - ./storage/exp4/ # storage
  - ./data/envs/generalization/seen/ #env_dir

method: grid
parameters:
  sampling_beta:
    values: [1, 10, 20, 30]
